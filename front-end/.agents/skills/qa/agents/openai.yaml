interface:
  display_name: "QA Frontend"
  short_description: "Executa lint, Vitest e build do frontend"
  default_prompt: "Use $qa para validar mudancas no frontend com lint, Vitest e build."
policy:
  allow_implicit_invocation: true
